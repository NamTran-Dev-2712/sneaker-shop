export enum SortBy {
  NAME = "name",
  CODE = "code",
  CREATED_AT = "createdAt",
  UPDATED_AT = "updatedAt",
}

export enum SortColorBy {
  NAME = "name",
  HEX = "hex",
  CREATED_AT = "createdAt",
}

export enum SortSizeBy {
  SYSTEM = "system",
  VALUE = "value",
  CREATED_AT = "createdAt",
}

export enum SortSellableItemBy {
  SKU = "sku",
  PRICE = "price",
  EFFECTIVE_FROM = "effectiveFrom",
  PRODUCT_NAME = "productName",
}
